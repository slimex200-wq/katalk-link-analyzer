# AI 백엔드 설정 (선택)
# 자동 감지: Claude CLI → Codex CLI → OpenAI API 순서
# 강제 지정: ANALYZER_BACKEND=claude / codex / openai
# ANALYZER_BACKEND=claude

# OpenAI API 사용 시에만 필요 (유료)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
